# Needed for providing root directory to use for compiling dataflow source codetg
# Really, the pyproject.toml file gets used to compile a package but this files existence proves to be a necessatiy
from setuptools import setup

setup()
